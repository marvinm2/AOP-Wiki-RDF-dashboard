---
phase: 01-foundation-and-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plots.py
  - app.py
  - plots/__init__.py
  - plots/shared.py
  - requirements.txt
  - README.md
  - docker-compose.yml
  - app.log
  - app_new.log
  - IMPROVEMENTS_SUMMARY.md
autonomous: true
requirements:
  - INFR-01
  - INFR-02

must_haves:
  truths:
    - "Application starts with zero import warnings and no reference to legacy plots.py"
    - "python -c 'import plots; print(plots.__file__)' prints plots/__init__.py, not plots.py"
    - "No commented-out boxplot_by_status references remain in app.py or plots/__init__.py"
    - "docker-compose up --build produces identical results on consecutive runs with all dependencies pinned"
    - "requirements.txt contains all 7 dependencies pinned to compatible-release ranges"
  artifacts:
    - path: "requirements.txt"
      provides: "Pinned dependency manifest"
      contains: "Flask~=3.1"
    - path: "app.py"
      provides: "Clean Flask app without dead imports or commented-out code"
    - path: "plots/__init__.py"
      provides: "Clean module exports without commented-out references"
  key_links:
    - from: "app.py"
      to: "plots/__init__.py"
      via: "from plots import ..."
      pattern: "from plots import"
    - from: "requirements.txt"
      to: "Dockerfile"
      via: "pip install -r requirements.txt"
      pattern: "requirements.txt"
---

<objective>
Remove all legacy code, dead imports, commented-out references, and stale files from the codebase, then pin all dependencies to tested compatible-release versions.

Purpose: Establish a clean baseline so that subsequent infrastructure changes (Gunicorn, caching, logging) apply to unambiguous, well-defined code without legacy interference.

Output: A codebase free of dead code with reproducible dependency installation.
</objective>

<execution_context>
@/home/marvin/.claude/get-shit-done/workflows/execute-plan.md
@/home/marvin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-cleanup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete legacy files and stale artifacts</name>
  <files>
    plots.py
    app.log
    app_new.log
    IMPROVEMENTS_SUMMARY.md
  </files>
  <action>
Delete the following files with separate, explanatory git commits per user decision for careful commit trail:

1. `plots.py` (4,194 lines) -- legacy monolith, all functionality migrated to `plots/` package. Commit message: "remove legacy plots.py monolith (4,194 lines) — all functionality lives in plots/ package"
2. `app.log` (8,003 bytes) -- stale log file. Commit message: "remove stale app.log"
3. `app_new.log` (11,094 bytes) -- stale log file. Commit message: "remove stale app_new.log"
4. `IMPROVEMENTS_SUMMARY.md` (11,487 bytes) -- historical record, not actively used. Commit message: "remove IMPROVEMENTS_SUMMARY.md — historical record, no longer maintained"

After deleting plots.py, verify: `python -c "import plots; print(plots.__file__)"` should print the `plots/__init__.py` path.

Note: `verify_properties.py` is untracked (shown in git status) — do NOT delete it via git since it is not tracked. Leave it as-is.

Do NOT delete `templates/index.html` or `templates/trends.html` — they are still referenced by active routes (`/old-dashboard`, `/dashboard`) and template includes respectively.
  </action>
  <verify>
- `ls plots.py app.log app_new.log IMPROVEMENTS_SUMMARY.md 2>&1` shows "No such file" for all four
- `python -c "import plots; print(plots.__file__)"` prints path ending in `plots/__init__.py`
- `git log --oneline -4` shows four separate cleanup commits
  </verify>
  <done>All four legacy/stale files are deleted with individual explanatory commits. plots/ package resolves correctly as the sole import target.</done>
</task>

<task type="auto">
  <name>Task 2: Clean dead code from app.py, plots/__init__.py, plots/shared.py and pin dependencies</name>
  <files>
    app.py
    plots/__init__.py
    plots/shared.py
    requirements.txt
    README.md
    docker-compose.yml
  </files>
  <action>
**Dead code cleanup in app.py:**

1. Consolidate duplicate Flask imports (lines 41 and 63) into a single import:
   `from flask import Flask, render_template, jsonify, request, Response, url_for, redirect`

2. Remove unused imports:
   - `import plotly.express as px` (never used in app.py)
   - `import plotly.io as pio` (never used in app.py)
   - `import os` (never used in app.py — if used, keep it; verify with grep)
   - `from functools import reduce` (never used in app.py)
   - `from SPARQLWrapper import SPARQLWrapper, JSON` (never used in app.py)

3. Remove all commented-out `boxplot_by_status` references:
   - Line ~88: `# plot_aop_completeness_boxplot_by_status,  # REMOVED`
   - Line ~182: `# REMOVED: 'aop_completeness_boxplot_by_status'`
   - Line ~256: `# graph_aop_completeness_boxplot_by_status = ""  # REMOVED`
   - Line ~1444: `# 'aop_completeness_boxplot_by_status': lambda: ...  # REMOVED`
   Search for ALL occurrences with `grep -n boxplot_by_status app.py` and remove every commented-out line.

**Dead code cleanup in plots/__init__.py:**

4. Remove all commented-out `boxplot_by_status` references:
   - Line ~163: `# plot_aop_completeness_boxplot_by_status,  # REMOVED`
   - Line ~235: `# 'plot_aop_completeness_boxplot_by_status',  # REMOVED`
   - Line ~279: `# 'plot_aop_completeness_boxplot_by_status',  # REMOVED`
   Search for ALL occurrences with `grep -n boxplot_by_status plots/__init__.py` and remove.

**Dead code cleanup in plots/shared.py:**

5. Remove unused import: `from functools import reduce` (only used in trends_plots.py, not shared.py)

**Pin dependencies in requirements.txt:**

6. Replace contents of `requirements.txt` with:
```
Flask~=3.1
pandas~=2.2
plotly~=5.22
SPARQLWrapper~=2.0
gunicorn~=25.1
python-json-logger~=2.0
requests~=2.32
```
Pin to compatible-release (`~=`) per user decision. Add `gunicorn`, `python-json-logger`, and `requests` which are used but not currently listed. `requests` is imported by shared.py directly.

**Update README.md:**

7. Remove references to `plots.py` in README.md:
   - Line ~109: Remove `plots.py` from project structure listing
   - Line ~131: Update "Create plot function in `plots.py`" to reference `plots/` package
   - Line ~137: Update "Modify `BRAND_COLORS` in `plots.py`" to reference `plots/shared.py`
   Search for all `plots.py` references and update.

**Update docker-compose.yml:**

8. Remove deprecated `version: '3.8'` key from docker-compose.yml. Docker Compose v2+ ignores this and shows a deprecation warning.

Commit all dead code cleanup as a single commit: "clean dead imports, commented-out code, and stale references"
Commit requirements.txt pinning separately: "pin all dependencies to compatible-release versions"
Commit README.md + docker-compose.yml updates separately: "update README and docker-compose for current codebase"
  </action>
  <verify>
- `python -c "from app import app; print('OK')"` succeeds without import warnings
- `grep -r "boxplot_by_status" app.py plots/__init__.py` returns no results
- `grep -c "from flask import" app.py` returns exactly 1
- `grep "plotly.express\|plotly.io\|from functools" app.py` returns no results
- `grep "from functools" plots/shared.py` returns no results
- `cat requirements.txt` shows all 7 dependencies with `~=` pinning
- `grep "plots.py" README.md` returns no results referring to the deleted monolith
- `grep "version:" docker-compose.yml` returns no results (deprecated key removed)
  </verify>
  <done>All dead imports removed from app.py and shared.py. All commented-out boxplot_by_status references removed from app.py and plots/__init__.py. Dependencies pinned to compatible-release ranges. README.md and docker-compose.yml updated for current codebase state.</done>
</task>

</tasks>

<verification>
1. Application starts cleanly: `python -c "from app import app; print('OK')"`
2. No legacy file references: `grep -r "plots\.py" . --include="*.py" --include="*.md" | grep -v ".planning/" | grep -v "__pycache__"` returns nothing pointing to the deleted monolith
3. Clean import resolution: `python -c "import plots; print(plots.__file__)"` prints `plots/__init__.py`
4. Dependencies pinned: `wc -l requirements.txt` shows 7 lines, all with `~=`
5. Docker compose clean: `docker-compose config 2>&1 | grep -i "version is obsolete"` returns nothing
</verification>

<success_criteria>
- Zero references to legacy `plots.py` in any Python or Markdown file (outside .planning/)
- No commented-out `boxplot_by_status` code anywhere
- No unused imports in `app.py` or `plots/shared.py`
- All 7 dependencies pinned with `~=` operator in `requirements.txt`
- Application imports and starts without warnings
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-cleanup/01-01-SUMMARY.md`
</output>
