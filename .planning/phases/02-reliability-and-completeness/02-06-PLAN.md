---
phase: 02-reliability-and-completeness
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - plots/trends_plots.py
  - templates/trends.html
  - static/data/methodology_notes.json
autonomous: true
gap_closure: true
requirements: [RELY-02, EXPL-07]

must_haves:
  truths:
    - "OECD completeness trend plot title and legend do not overlap on typical screen widths"
    - "AOPs Modified Over Time and AOP Creation vs Modification plot boxes each have an expandable methodology note"
    - "Methodology note limitations contain only researcher-relevant information, no implementation-detail noise like query times"
  artifacts:
    - path: "plots/trends_plots.py"
      provides: "Fixed OECD plot layout with adequate legend spacing"
      contains: "margin.*t=1[0-9][0-9]|legend.*y.*1\\."
    - path: "templates/trends.html"
      provides: "Methodology note calls on all AOP Lifetime sub-plot boxes"
      contains: "methodology_note.*aop_lifetime"
    - path: "static/data/methodology_notes.json"
      provides: "Cleaned limitations text across all 28 entries"
  key_links:
    - from: "templates/trends.html"
      to: "static/data/methodology_notes.json"
      via: "Jinja2 macro rendering from methodology_notes context variable"
      pattern: "methodology_note\\(methodology_notes"
---

<objective>
Fix three WARNING gaps from Phase 2 verification plus user feedback on methodology note quality: (1) OECD plot title/legend overlap, (2) two AOP Lifetime plot boxes missing methodology notes, (3) stale/inappropriate limitations text across methodology notes.

Purpose: Complete the EXPL-07 requirement (every plot has methodology notes) and ensure all visual elements render without overlap.
Output: Clean OECD plot layout, complete methodology note coverage, researcher-appropriate limitations text.
</objective>

<execution_context>
@/home/marvin/.claude/get-shit-done/workflows/execute-plan.md
@/home/marvin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-reliability-and-completeness/02-03-SUMMARY.md
@.planning/phases/02-reliability-and-completeness/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix OECD plot layout and add missing methodology notes to AOP Lifetime section</name>
  <files>plots/trends_plots.py, templates/trends.html</files>
  <action>
**Part A: Fix OECD completeness trend plot title/legend overlap.**

In `plots/trends_plots.py`, function `plot_oecd_completeness_trend()` (~line 2910), the current layout is:
```python
margin=dict(l=50, r=20, t=50, b=50),
legend=dict(
    title="OECD Status",
    orientation="h",
    yanchor="bottom",
    y=1.02,
    xanchor="right",
    x=1
)
```

The horizontal legend at y=1.02 overlaps the title "Mean AOP Completeness by OECD Status Over Time" because t=50 does not leave room above the plot for both.

Fix by increasing the top margin to 120 and adjusting legend position:
```python
margin=dict(l=50, r=20, t=120, b=50),
legend=dict(
    title="OECD Status",
    orientation="h",
    yanchor="bottom",
    y=1.15,
    xanchor="center",
    x=0.5
)
```

This positions the legend centered above the chart with enough margin to clear the title.

Also fix the entity completeness trends plot which has the same pattern (~line 2327):
```python
legend=dict(title="Entity Type", orientation="h", yanchor="bottom", y=1.02, xanchor="right", x=1)
```
Check its margin — if also t=50, increase to t=100 and adjust y to 1.10.

**Part B: Add methodology notes to the two missing AOP Lifetime plot boxes.**

In `templates/trends.html`, the AOP Lifetime section has three plot boxes:
1. "AOPs Created Over Time" (~line 468) — HAS methodology note `{{ methodology_note(methodology_notes, 'aop_lifetime') }}`
2. "AOPs Modified Over Time" (~line 478) — MISSING methodology note
3. "AOP Creation vs Modification" (~line 498) — MISSING methodology note

Add `{{ methodology_note(methodology_notes, 'aop_lifetime') }}` after the `</p>` closing tag of the plot-description paragraph in both missing boxes:

For "AOPs Modified Over Time" (~line 492, after `</p>`):
```html
        </p>
        {{ methodology_note(methodology_notes, 'aop_lifetime') }}
        <div class="lazy-plot" ...>
```

For "AOP Creation vs Modification" (~line 513, after `</p>`):
```html
        </p>
        {{ methodology_note(methodology_notes, 'aop_lifetime') }}
        <div class="lazy-plot" ...>
```

Using the same 'aop_lifetime' key is correct — this was the established decision from 02-03: "Bio object annotations and AOPs modified/scatter plots share the aop_lifetime methodology key since they are part of the same logical section."
  </action>
  <verify>
1. Start the application and navigate to /trends
2. Scroll to "OECD Completeness Trend" — verify title and legend are clearly separated without overlap
3. Scroll to "AOP Lifetime" section — verify all three plot boxes (AOPs Created, AOPs Modified, AOP Creation vs Modification) have expandable "Methodology" sections
4. Count total methodology_note calls in trends.html — should be 19 (was 17, added 2)
  </verify>
  <done>OECD plot renders without title/legend overlap, and all three AOP Lifetime plot boxes have expandable methodology notes.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up methodology note limitations across all entries</name>
  <files>static/data/methodology_notes.json</files>
  <action>
Review and clean ALL 28 entries in `static/data/methodology_notes.json` based on user feedback:

**Specific fixes required:**

1. **aop_completeness_boxplot** (line 125): Remove "Query may take 30-75 seconds on large datasets." from limitations. The plot is pre-computed at startup; this text is stale and misleading. Replace with nothing — just delete the sentence, keeping the remaining limitations about composite scoring methodology.

   Before: `"Composite scores weight AOP, KE, and KER completeness equally. Excludes 100%-present properties. Box plots show quartiles (Q1, median, Q3) with whiskers at 1.5x IQR. AOPs with no linked KEs or KERs use only their own completeness. Query may take 30-75 seconds on large datasets."`

   After: `"Composite scores weight AOP, KE, and KER completeness equally. Excludes 100%-present properties. Box plots show quartiles (Q1, median, Q3) with whiskers at 1.5x IQR. AOPs with no linked KEs or KERs use only their own completeness."`

**General cleanup principles (apply across ALL 28 entries):**

2. **Remove performance-related limitations** — sentences about query speed, computation time, or server-side performance are implementation details irrelevant to researchers using the dashboard. Look for patterns like "may take X seconds", "requires Y computation", "query performance".

3. **Remove redundant limitations that appear in multiple entries** — where the same exact sentence (e.g., "Measures presence only, not quality of property values.") appears in 3+ entries, keep it only where it is most contextually important and remove from others where it adds no new information. Use judgment: if a user is looking at a specific plot's methodology, they should understand the key caveats for THAT plot without needing to read all others.

4. **Keep all data-quality and methodological limitations** — sentences about what the data does/does not capture, statistical caveats (averages masking distributions, equal weighting), ontology classification heuristics, and scope boundaries. These are researcher-relevant.

5. **Keep all data-source scope limitations** — sentences about what is excluded (e.g., "KEs not linked to any AOP are excluded").

6. **Ensure consistent tone** — limitations should read as scientific caveats, not system-admin warnings. For example:
   - BAD: "Query may take 30-75 seconds on large datasets" (system concern)
   - GOOD: "AOPs with no linked KEs or KERs use only their own completeness" (methodological caveat)

Read the full JSON file, edit each entry's limitations field applying the above rules, and write the updated file.
  </action>
  <verify>
1. Read the updated methodology_notes.json and confirm:
   - No entry contains text about query times, computation speed, or performance
   - The aop_completeness_boxplot limitations no longer mentions "30-75 seconds"
   - All 28 entries still have non-empty limitations fields
   - Limitations text reads as scientific methodology caveats
2. Start the application and spot-check 3-4 methodology notes on the dashboard — they should display cleanly without implementation jargon
  </verify>
  <done>All 28 methodology note entries have clean, researcher-appropriate limitations without performance-related or redundant text. Every limitations field remains non-empty and describes relevant data-quality or methodological caveats.</done>
</task>

</tasks>

<verification>
1. Start the application with `python app.py` — no errors
2. Navigate to /trends, OECD Completeness Trend section — title and legend visually separated
3. Navigate to /trends, AOP Lifetime section — all 3 sub-plot boxes show expandable methodology notes
4. Click any methodology note — content is clean, no performance/system jargon
5. Count methodology_note calls: `grep -c "methodology_note" templates/trends.html` — should return 19
6. Verify JSON is valid: `python -c "import json; json.load(open('static/data/methodology_notes.json'))"`
</verification>

<success_criteria>
- OECD plot title and horizontal legend are visually separated with clear whitespace on a 1920px-wide viewport
- 19 methodology_note() calls in trends.html (up from 17)
- No methodology note limitations field contains text about query performance, computation time, or server-side implementation
- All 28 methodology note entries retain non-empty, researcher-relevant limitations
- JSON file parses without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-reliability-and-completeness/02-06-SUMMARY.md`
</output>
