---
phase: 02-reliability-and-completeness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - app.py
  - plots/trends_plots.py
  - plots/__init__.py
  - templates/trends.html
  - config.py
autonomous: true
requirements:
  - RELY-01
  - RELY-02

must_haves:
  truths:
    - "AOP Completeness Distribution boxplot loads instantly (pre-computed at startup) rather than taking ~75 seconds on-demand"
    - "OECD status completeness trend is visible on the trends page as a line chart showing mean completeness per status across versions"
    - "The OECD completeness trend plot has download buttons for CSV, PNG, and SVG"
    - "kaleido is listed in requirements.txt so PNG/SVG export works in Docker"
  artifacts:
    - path: "requirements.txt"
      provides: "kaleido dependency for static image export"
      contains: "kaleido"
    - path: "plots/trends_plots.py"
      provides: "plot_oecd_completeness_trend function"
      contains: "def plot_oecd_completeness_trend"
    - path: "app.py"
      provides: "Boxplot in startup computation, OECD trend integration"
      contains: "oecd_completeness_trend"
    - path: "templates/trends.html"
      provides: "OECD completeness trend plot container with download buttons"
      contains: "oecd-completeness-trend"
  key_links:
    - from: "plots/trends_plots.py"
      to: "plots/__init__.py"
      via: "export of plot_oecd_completeness_trend"
      pattern: "plot_oecd_completeness_trend"
    - from: "app.py"
      to: "plots/__init__.py"
      via: "import of plot_oecd_completeness_trend"
      pattern: "from plots import.*plot_oecd_completeness_trend"
    - from: "app.py"
      to: "compute_plots_parallel"
      via: "boxplot moved from on-demand lambda to startup plot_tasks list"
      pattern: "aop_completeness_boxplot.*safe_plot_execution"
---

<objective>
Optimize the AOP Completeness Distribution boxplot to load instantly and restore the OECD status visualization as a grouped bar chart with historical trends.

Purpose: The boxplot currently takes ~75s on first load (lazy-loaded on-demand). Moving it to startup computation means 0s user-facing load time. The OECD status visualization was removed because the boxplot approach hit Virtuoso limits; the replacement uses aggregated means (75 data points vs 240K rows), avoiding the limit entirely.

Output: Pre-computed boxplot, new `plot_oecd_completeness_trend()` function, kaleido in requirements.txt, SPARQL timeout tuning.
</objective>

<execution_context>
@/home/marvin/.claude/get-shit-done/workflows/execute-plan.md
@/home/marvin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-reliability-and-completeness/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-compute boxplot at startup and add kaleido dependency</name>
  <files>requirements.txt, app.py, config.py</files>
  <action>
1. Add `kaleido~=0.2` to requirements.txt (after plotly line). This is required for PNG/SVG export via `pio.to_image()` — it is imported by Plotly internally but was never added to the manifest, so Docker builds fail on export.

2. In app.py, the boxplot is currently skipped at startup (line ~256: `graph_aop_completeness_boxplot = ""`). It is NOT in the `plot_tasks` list — it is generated on-demand via a lambda in `plot_map`. Fix this:
   - The boxplot task `('aop_completeness_boxplot', lambda: safe_plot_execution(plot_aop_completeness_boxplot))` is ALREADY in `plot_tasks` (line ~173). Verify this is the case.
   - Remove the line `graph_aop_completeness_boxplot = ""` (around line 256) — the result should come from `plot_results` like all other plots.
   - Update the results extraction to properly unpack the boxplot result from `plot_results`:
     ```python
     graph_aop_completeness_boxplot = plot_results.get('aop_completeness_boxplot') or ""
     ```
   - This means the boxplot is computed at startup in parallel with all other plots, trading boot time for 0s user-facing load time (user-approved tradeoff).

3. In config.py, add a `SPARQL_SLOW_TIMEOUT` config variable (default: 60 seconds) for complex queries that legitimately take longer. Also increase `PLOT_TIMEOUT` from 60 to 120 seconds to accommodate the completeness boxplot during startup. Add:
   ```python
   SPARQL_SLOW_TIMEOUT = int(os.environ.get('SPARQL_SLOW_TIMEOUT', '60'))
   PLOT_TIMEOUT = int(os.environ.get('PLOT_TIMEOUT', '120'))
   ```
   Make sure PLOT_TIMEOUT is updated from the existing value of 60 (not duplicated).
  </action>
  <verify>
- `grep kaleido requirements.txt` returns the kaleido line
- `grep 'graph_aop_completeness_boxplot = ""' app.py` returns NO matches (the skip line is removed)
- `grep SPARQL_SLOW_TIMEOUT config.py` returns the new config variable
- `grep 'PLOT_TIMEOUT.*120' config.py` confirms the increased timeout
- `python -c "from config import Config; print(Config.PLOT_TIMEOUT)"` prints 120
  </verify>
  <done>Boxplot is pre-computed at startup via plot_tasks (not skipped), kaleido is in requirements.txt, SPARQL_SLOW_TIMEOUT is configurable, and PLOT_TIMEOUT is 120s.</done>
</task>

<task type="auto">
  <name>Task 2: Create OECD completeness trend visualization</name>
  <files>plots/trends_plots.py, plots/__init__.py, app.py, templates/trends.html</files>
  <action>
1. In plots/trends_plots.py, create `plot_oecd_completeness_trend()` function. This replaces the removed `plot_aop_completeness_boxplot_by_status()` that hit Virtuoso limits.

   **Strategy:** For each version, compute mean completeness per OECD status. The existing `plot_latest_aop_completeness_by_status()` in latest_plots.py (line ~1216) already does this for a single version — reuse its SPARQL aggregation pattern but loop over all versions.

   **Implementation:**
   - Get all versions via `get_all_versions()`
   - For each version, query mean completeness per OECD status (aggregated in SPARQL using GROUP BY, not raw per-AOP scores). This produces ~5 rows per version (one per status), not 240K raw scores.
   - Use the same property list from `property_labels.csv` that other completeness functions use.
   - Combine results into a DataFrame: columns = [version, status, mean_completeness]
   - Create a Plotly line chart: x=version date, y=mean_completeness, color=OECD status
   - Include all OECD statuses found in data (no grouping into "Other") per user decision
   - Use VHP4Safety brand colors (BRAND_COLORS from shared.py)
   - Cache both data and figure:
     ```python
     _plot_data_cache['oecd_completeness_trend'] = df.copy()
     _plot_figure_cache['oecd_completeness_trend'] = fig
     ```
   - Return the HTML string of the plot
   - Return type: `str` (single plot, no abs/delta pair)
   - Wrap in try/except with `create_fallback_plot()` on error

   **SPARQL query pattern (per version):**
   Query the version's named graph for AOPs, their OECD status, and count their properties. Compute mean completeness per status in Python from per-AOP property counts (matching the approach in `plot_latest_aop_completeness_by_status`). Key: only transfer per-AOP property counts, then aggregate to means in Python. Do NOT try to get raw completeness for all AOPs across all versions in a single query.

   If the per-version approach is too slow (>30s per version * 15 versions), use ThreadPoolExecutor to query versions in parallel (3-5 workers) with the existing `run_sparql_query_with_retry()`.

2. Export from plots/__init__.py:
   - Add `plot_oecd_completeness_trend` to the trends_plots import block
   - Add to `__all__` list
   - Add to `get_available_functions()['historical_trends']`

3. Register in app.py:
   - Add to imports from plots
   - Add to `plot_tasks`: `('oecd_completeness_trend', lambda: safe_plot_execution(plot_oecd_completeness_trend))`
   - Extract result: `graph_oecd_completeness_trend = plot_results.get('oecd_completeness_trend') or ""`
   - Add to `plot_map` dictionary for lazy-loading API
   - Pass to `render_template()` for trends page

4. Add template container in trends.html:
   - Uncomment/replace the removed OECD status block (lines 308-334, currently HTML-commented)
   - New block should use the same pattern as other single-plot boxes (like entity_completeness_trends or aop_network_density)
   - Include download dropdown with CSV/PNG/SVG links pointing to `/download/trend/oecd_completeness_trend?format=csv|png|svg`
   - Give it id="oecd-completeness-trend" and place it after the existing boxplot in the entity-completeness section
   - Description: "Shows the mean completeness score per OECD approval status over time. Tracks how documentation quality correlates with maturity level across database versions."
  </action>
  <verify>
- `grep 'def plot_oecd_completeness_trend' plots/trends_plots.py` finds the function
- `grep 'plot_oecd_completeness_trend' plots/__init__.py` finds the export
- `grep 'oecd_completeness_trend' app.py` finds import, plot_tasks entry, and result extraction
- `grep 'oecd-completeness-trend' templates/trends.html` finds the template container
- `python -c "from plots import plot_oecd_completeness_trend; print('import OK')"` succeeds
  </verify>
  <done>OECD completeness trend visualization is fully integrated: new plot function computes mean completeness per OECD status across all versions, exported from plots package, registered in app.py startup computation, and visible on trends page with download buttons.</done>
</task>

</tasks>

<verification>
1. The AOP Completeness Distribution boxplot is in `plot_tasks` (computed at startup, not on-demand)
2. The `graph_aop_completeness_boxplot = ""` skip line is removed from app.py
3. `plot_oecd_completeness_trend()` exists, is exported, and is registered in app.py
4. The OECD trend container is visible in trends.html (not commented out)
5. kaleido is in requirements.txt
6. SPARQL_SLOW_TIMEOUT and updated PLOT_TIMEOUT exist in config.py
</verification>

<success_criteria>
- Boxplot computed at startup means 0s user-facing load time (success criterion RELY-01: under 30 seconds)
- OECD completeness trend plot loads without hitting Virtuoso limits (success criterion RELY-02)
- kaleido available for PNG/SVG export in Docker
</success_criteria>

<output>
After completion, create `.planning/phases/02-reliability-and-completeness/02-01-SUMMARY.md`
</output>
