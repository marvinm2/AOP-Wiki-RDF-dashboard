---
phase: 06-vhp-platform-deployment
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - stack.yml
  - DEPLOY.md
autonomous: false
requirements:
  - INFR-06
user_setup:
  - service: VHP4Safety Cluster
    why: "Docker Swarm deployment target"
    env_vars:
      - name: DBA_PASSWORD
        source: "Choose a Virtuoso admin password for the .env file on TGX1"
    dashboard_config:
      - task: "Request DNS A records for both subdomains"
        location: "VHP4Safety DNS admin: aopwiki-dashboard.cloud.vhp4safety.nl -> 81.169.246.233, aopwiki-multirdf.vhp4safety.nl -> 81.169.246.233"
      - task: "Create GlusterFS data directory"
        location: "SSH to TGX1: mkdir -p /mnt/gluster/aopwiki-dashboard/virtuoso-data"
      - task: "Run Setup pipeline to load RDF data"
        location: "On TGX1: run the AOP-Wiki-RDF Setup pipeline to populate /mnt/gluster/aopwiki-dashboard/virtuoso-data"
must_haves:
  truths:
    - "stack.yml defines both dashboard and virtuoso services in a single Swarm stack"
    - "Traefik labels route aopwiki-dashboard.cloud.vhp4safety.nl to dashboard service on port 5000"
    - "Traefik labels route aopwiki-multirdf.vhp4safety.nl to virtuoso service on port 8890"
    - "Dashboard connects to Virtuoso via internal overlay network using service name DNS"
    - "Both services are constrained to TGX1 node"
    - "Health probes have start_period >= 120s to tolerate plot precomputation"
    - "Deployment instructions cover the complete scp + build + deploy workflow"
  artifacts:
    - path: "stack.yml"
      provides: "Docker Swarm stack definition for dashboard + Virtuoso"
      contains: "docker stack deploy"
    - path: "DEPLOY.md"
      provides: "Step-by-step deployment instructions"
      contains: "docker stack deploy"
  key_links:
    - from: "stack.yml dashboard service"
      to: "stack.yml virtuoso service"
      via: "SPARQL_ENDPOINT=http://virtuoso:8890/sparql on internal network"
      pattern: "SPARQL_ENDPOINT.*virtuoso.*8890"
    - from: "stack.yml dashboard service"
      to: "Traefik"
      via: "deploy.labels with Host rule"
      pattern: "aopwiki-dashboard.cloud.vhp4safety.nl"
    - from: "stack.yml virtuoso service"
      to: "GlusterFS"
      via: "volume mount"
      pattern: "/mnt/gluster/aopwiki-dashboard/virtuoso-data"
---

<objective>
Create the Docker Swarm stack file and deployment instructions for the VHP4Safety platform.

Purpose: This is the final deployment artifact -- a `stack.yml` that defines the complete multi-container stack (dashboard + Virtuoso) with Traefik routing, health probes, and network configuration, plus a `DEPLOY.md` with step-by-step instructions for the SSH-based deployment workflow.

Output: stack.yml (Swarm stack definition), DEPLOY.md (deployment guide)
</objective>

<execution_context>
@/home/marvin/.claude/get-shit-done/workflows/execute-plan.md
@/home/marvin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-vhp-platform-deployment/06-CONTEXT.md
@.planning/phases/06-vhp-platform-deployment/06-RESEARCH.md
@.planning/phases/06-vhp-platform-deployment/06-01-SUMMARY.md

@Dockerfile
@gunicorn.conf.py
@docker-compose.yml

<interfaces>
<!-- From 06-RESEARCH.md: exact stack pattern to follow -->

Stack file pattern (from developer-workflow.md Section 5):
- version: "3.8"
- Two networks: core (external: true) for Traefik, internal (driver: overlay) for inter-service
- deploy.placement.constraints for node pinning
- deploy.labels for Traefik routing (not service-level labels)
- deploy.restart_policy.condition: on-failure
- deploy.resources for memory limits
- healthcheck with start_period for slow-starting services

From config.py:
```python
SPARQL_ENDPOINT = os.getenv("SPARQL_ENDPOINT", "http://localhost:8890/sparql")
```

From gunicorn.conf.py:
```python
bind = "0.0.0.0:5000"
workers = 2
worker_class = "gthread"
threads = 4
preload_app = True
timeout = 120
worker_tmp_dir = "/dev/shm"
```

Deployment constraints (from CONTEXT.md):
- SSH as mmartens user (no sudo, docker group)
- Build locally on TGX1 (no registry)
- Node constraint: node.hostname==h3068544.stratoserver.net
- Dashboard domain: aopwiki-dashboard.cloud.vhp4safety.nl
- Virtuoso domain: aopwiki-multirdf.vhp4safety.nl
- GlusterFS path: /mnt/gluster/aopwiki-dashboard/virtuoso-data
- Virtuoso healthcheck uses wget (curl not in image)
- Dashboard healthcheck uses Python urllib (wget/curl not in slim image)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create stack.yml and DEPLOY.md</name>
  <files>stack.yml, DEPLOY.md</files>
  <action>
Create `stack.yml` (Docker Swarm stack definition) with the following structure. Follow the pattern from 06-RESEARCH.md exactly:

**Networks:**
- `core`: external: true (Traefik overlay)
- `internal`: driver: overlay (Virtuoso-dashboard communication)

**Service: dashboard**
- image: aopwiki-dashboard:latest
- networks: core, internal
- environment: SPARQL_ENDPOINT=http://virtuoso:8890/sparql
- deploy.placement.constraints: node.hostname==h3068544.stratoserver.net
- deploy.restart_policy.condition: on-failure
- deploy.labels (Traefik):
  - traefik.enable=true
  - traefik.http.routers.aopwiki-dashboard.rule=Host(`aopwiki-dashboard.cloud.vhp4safety.nl`)
  - traefik.http.routers.aopwiki-dashboard.entrypoints=websecure
  - traefik.http.routers.aopwiki-dashboard.tls=true
  - traefik.http.routers.aopwiki-dashboard.tls.certresolver=letsencrypt
  - traefik.http.services.aopwiki-dashboard.loadbalancer.server.port=5000
  - traefik.docker.network=core
- deploy.resources: limits 2G memory, reservations 512M
- healthcheck: Python urllib check against localhost:5000/health, interval 30s, timeout 10s, retries 3, start_period 120s

**Service: virtuoso**
- image: openlink/virtuoso-opensource-7
- networks: core, internal
- environment: DBA_PASSWORD=${DBA_PASSWORD}, VIRT_Parameters_DirsAllowed="., ../vad, /usr/share/proj, /database/data"
- volumes: /mnt/gluster/aopwiki-dashboard/virtuoso-data:/database/data
- deploy.placement.constraints: node.hostname==h3068544.stratoserver.net
- deploy.restart_policy.condition: on-failure
- deploy.labels (Traefik):
  - traefik.enable=true
  - traefik.http.routers.aopwiki-multirdf.rule=Host(`aopwiki-multirdf.vhp4safety.nl`)
  - traefik.http.routers.aopwiki-multirdf.entrypoints=websecure
  - traefik.http.routers.aopwiki-multirdf.tls=true
  - traefik.http.routers.aopwiki-multirdf.tls.certresolver=letsencrypt
  - traefik.http.services.aopwiki-multirdf.loadbalancer.server.port=8890
  - traefik.docker.network=core
- deploy.resources: limits 4G memory, reservations 2G
- healthcheck: wget against localhost:8890/sparql?query=ASK+%7B%7D, interval 15s, timeout 5s, retries 20, start_period 60s

Do NOT include `ports:`, `container_name:`, `restart:`, or `build:` keys (these are Compose-only, Swarm ignores them).

**Create DEPLOY.md** with step-by-step deployment instructions:

1. Prerequisites section: DNS records needed, GlusterFS directory, RDF data loaded via Setup pipeline, mmartens SSH access
2. Build section: scp project to TGX1, SSH in, docker build -t aopwiki-dashboard:latest .
3. Deploy section: create .env with DBA_PASSWORD, docker stack deploy -c stack.yml aopwiki-dashboard
4. Verify section: docker service ls, docker service logs, curl health endpoint
5. Update section: rebuild image, docker service update --force aopwiki-dashboard_dashboard
6. Troubleshooting section: common issues (DNS not resolved, Virtuoso not starting, health probe failures)

Keep DEPLOY.md concise and practical -- this is an operations reference, not documentation for documentation's sake.
  </action>
  <verify>
    <automated>cd /home/marvin/Documents/Services/AOP-Wiki_services/AOPWikiRDF-multiEndpoint/AOP-Wiki-RDF-dashboard && python -c "
import yaml
with open('stack.yml') as f:
    stack = yaml.safe_load(f)
svcs = stack['services']
assert 'dashboard' in svcs, 'Missing dashboard service'
assert 'virtuoso' in svcs, 'Missing virtuoso service'
assert 'core' in stack['networks'], 'Missing core network'
assert 'internal' in stack['networks'], 'Missing internal network'
assert 'SPARQL_ENDPOINT=http://virtuoso:8890/sparql' in svcs['dashboard']['environment'], 'Wrong SPARQL_ENDPOINT'
assert '/mnt/gluster/aopwiki-dashboard/virtuoso-data:/database/data' in svcs['virtuoso']['volumes'], 'Missing GlusterFS mount'
assert 'ports' not in svcs['dashboard'], 'dashboard should not have ports (Traefik handles routing)'
assert 'ports' not in svcs['virtuoso'], 'virtuoso should not have ports (Traefik handles routing)'
print('stack.yml validation passed')
" && test -f DEPLOY.md && grep -q "docker stack deploy" DEPLOY.md && echo "DEPLOY.md exists with deploy instructions"</automated>
  </verify>
  <done>
  - stack.yml defines dashboard + virtuoso services with Traefik labels, health probes, networks, constraints
  - No Compose-only keys (ports, build, container_name, restart)
  - DEPLOY.md has complete deployment workflow from scp to verification
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify deployment artifacts</name>
  <action>
Present the complete deployment artifacts to the user for review. This checkpoint verifies that stack.yml matches the user's cluster configuration and DEPLOY.md covers the deployment workflow accurately.

What was built:
1. `stack.yml` -- Docker Swarm stack with dashboard + Virtuoso, Traefik routing, health probes
2. `DEPLOY.md` -- Step-by-step deployment guide
3. CORS headers on all routes (from Plan 01)
4. Startup-aware health endpoint (from Plan 01)
5. Production SPARQL endpoint link on landing page (from Plan 01)

How to verify:
1. Review `stack.yml` for correctness against your cluster setup:
   - Service names match expected convention
   - Traefik labels match your router/certresolver names
   - Memory limits are appropriate for TGX1
   - GlusterFS path is correct
   - Node hostname constraint matches

2. Review `DEPLOY.md` for completeness:
   - Prerequisites are accurate
   - SSH/build/deploy commands look correct
   - Troubleshooting covers expected issues

3. Verify CORS locally (optional):
   ```bash
   curl -I http://localhost:5050/ | grep -i access-control
   ```

4. Verify health endpoint locally (optional):
   ```bash
   curl http://localhost:5050/health
   ```

5. Check landing page SPARQL link:
   - Visit landing page, confirm SPARQL Endpoint link points to https://aopwiki-multirdf.vhp4safety.nl/sparql

Resume signal: Type "approved" if deployment artifacts are ready for use, or describe any issues with the stack configuration.
  </action>
  <verify>User confirms deployment artifacts are correct</verify>
  <done>User has reviewed and approved stack.yml configuration and DEPLOY.md instructions</done>
</task>

</tasks>

<verification>
- `stack.yml` passes YAML validation and contains both services
- Traefik labels use correct domain names per user decision
- No Compose-only keys in stack file
- DEPLOY.md covers full deployment lifecycle
- All Phase 6 success criteria from ROADMAP are addressed
</verification>

<success_criteria>
- stack.yml is a valid Swarm stack with dashboard + Virtuoso services
- Traefik routes aopwiki-dashboard.cloud.vhp4safety.nl to dashboard:5000
- Traefik routes aopwiki-multirdf.vhp4safety.nl to virtuoso:8890
- Dashboard healthcheck has start_period >= 120s
- DEPLOY.md provides actionable deployment steps
- User approves deployment artifacts at checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/06-vhp-platform-deployment/06-02-SUMMARY.md`
</output>
