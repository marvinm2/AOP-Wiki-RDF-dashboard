---
phase: 06-vhp-platform-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - app.py
  - templates/landing.html
autonomous: true
requirements:
  - INFR-05
must_haves:
  truths:
    - "Cross-origin requests from any domain receive Access-Control-Allow-Origin: * header"
    - "Health endpoint returns 503 with status 'starting' during plot precomputation"
    - "Health endpoint returns 200 with status 'healthy' after startup when SPARQL is up"
    - "Health endpoint returns 503 with status 'unhealthy' if SPARQL goes down after startup"
    - "Landing page SPARQL endpoint link points to https://aopwiki-multirdf.vhp4safety.nl/sparql"
  artifacts:
    - path: "requirements.txt"
      provides: "flask-cors dependency"
      contains: "flask-cors"
    - path: "app.py"
      provides: "CORS initialization and startup-aware health endpoint"
      contains: "CORS(app)"
    - path: "templates/landing.html"
      provides: "Production SPARQL endpoint link"
      contains: "aopwiki-multirdf.vhp4safety.nl"
  key_links:
    - from: "app.py"
      to: "flask_cors"
      via: "CORS(app) initialization"
      pattern: "from flask_cors import CORS"
    - from: "app.py"
      to: "/health"
      via: "_startup_complete flag set after compute_plots_parallel()"
      pattern: "_startup_complete"
---

<objective>
Add CORS support, enhance the health endpoint with startup awareness, and update the landing page SPARQL link for production deployment.

Purpose: These application-level changes are prerequisites for the VHP4Safety Swarm deployment -- CORS enables cross-origin platform integration, the startup-aware health probe prevents premature container restarts during the ~75-second plot precomputation, and the SPARQL link connects users to the production endpoint.

Output: Modified app.py (CORS + health), updated requirements.txt, updated landing.html
</objective>

<execution_context>
@/home/marvin/.claude/get-shit-done/workflows/execute-plan.md
@/home/marvin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-vhp-platform-deployment/06-CONTEXT.md
@.planning/phases/06-vhp-platform-deployment/06-RESEARCH.md

@app.py
@requirements.txt
@templates/landing.html
@config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add flask-cors and CORS initialization</name>
  <files>requirements.txt, app.py</files>
  <action>
1. Add `flask-cors~=5.0` to `requirements.txt` (after the existing Flask line).

2. In `app.py`, add the CORS import and initialization:
   - After the existing Flask import line (`from flask import Flask, render_template, ...`), add: `from flask_cors import CORS`
   - After the existing `app = Flask(__name__)` line, add: `CORS(app)` with a comment: `# Wildcard CORS for VHP4Safety cross-origin access`
   - This enables `Access-Control-Allow-Origin: *` on all routes per user decision (public, read-only dashboard).

3. In `app.py`, enhance the `/health` endpoint with startup awareness:
   - Add a module-level flag `_startup_complete = False` BEFORE the `compute_plots_parallel()` call (around line 230, before `plot_results = compute_plots_parallel()`).
   - AFTER the `plot_results = compute_plots_parallel()` line (line 234), add: `_startup_complete = True`
   - Modify the `health_check()` function to check `_startup_complete` first. If False, return `{"status": "starting", "message": "Precomputing visualizations", "timestamp": time.time()}, 503`.
   - Keep the existing healthy/unhealthy logic for when `_startup_complete` is True.
   - The existing exception handler stays as-is.

4. Run `pip install flask-cors~=5.0` to install locally for testing.
  </action>
  <verify>
    <automated>cd /home/marvin/Documents/Services/AOP-Wiki_services/AOPWikiRDF-multiEndpoint/AOP-Wiki-RDF-dashboard && python -c "from flask_cors import CORS; print('flask-cors imported OK')" && grep -q "flask-cors" requirements.txt && grep -q "CORS(app)" app.py && grep -q "_startup_complete" app.py && echo "All checks passed"</automated>
  </verify>
  <done>
  - requirements.txt contains flask-cors~=5.0
  - app.py imports and initializes CORS(app)
  - app.py has _startup_complete flag set before/after compute_plots_parallel()
  - health_check() returns 503 with "starting" status when _startup_complete is False
  </done>
</task>

<task type="auto">
  <name>Task 2: Update landing page SPARQL endpoint link</name>
  <files>templates/landing.html</files>
  <action>
In `templates/landing.html` line 78, replace:
```html
<a href="#" title="Coming soon">SPARQL Endpoint (coming soon)</a>
```
with:
```html
<a href="https://aopwiki-multirdf.vhp4safety.nl/sparql" target="_blank" rel="noopener">SPARQL Endpoint</a>
```

This updates the placeholder link to point to the production Virtuoso SPARQL UI per user decision. Remove the "Coming soon" title and parenthetical since it will be live.
  </action>
  <verify>
    <automated>grep -q "aopwiki-multirdf.vhp4safety.nl/sparql" /home/marvin/Documents/Services/AOP-Wiki_services/AOPWikiRDF-multiEndpoint/AOP-Wiki-RDF-dashboard/templates/landing.html && ! grep -q "coming soon" /home/marvin/Documents/Services/AOP-Wiki_services/AOPWikiRDF-multiEndpoint/AOP-Wiki-RDF-dashboard/templates/landing.html && echo "Landing page link updated"</automated>
  </verify>
  <done>
  - Landing page links to https://aopwiki-multirdf.vhp4safety.nl/sparql
  - No "coming soon" text remains
  - Link opens in new tab with rel="noopener"
  </done>
</task>

</tasks>

<verification>
- `grep "flask-cors" requirements.txt` shows the dependency
- `grep "CORS(app)" app.py` shows CORS initialization
- `grep "_startup_complete" app.py` shows startup awareness flag
- `grep "aopwiki-multirdf" templates/landing.html` shows production SPARQL link
- `python -c "from app import app; print('App imports OK')"` confirms no import errors (run from project root)
</verification>

<success_criteria>
- flask-cors is listed in requirements.txt and importable
- All Flask routes return Access-Control-Allow-Origin: * header
- /health returns 503 during startup, 200 when healthy, 503 when SPARQL down
- Landing page SPARQL link points to production Virtuoso URL
</success_criteria>

<output>
After completion, create `.planning/phases/06-vhp-platform-deployment/06-01-SUMMARY.md`
</output>
