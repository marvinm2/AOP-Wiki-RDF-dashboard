---
phase: 07-plot-audit
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - .planning/phases/07-plot-audit/AUDIT-TRENDS.md
  - .planning/phases/07-plot-audit/AUDIT-REPORT.md
autonomous: true
requirements: [AUDIT-01, AUDIT-02, AUDIT-03]

must_haves:
  truths:
    - "User can see every trends_plots.py function evaluated against the 8-dimension binary rubric"
    - "User can see FIX NOW / FIX LATER / SKIP classification for every trends plot"
    - "User can see a per-plot color decision for every trends plot"
    - "User can read a single consolidated AUDIT-REPORT.md that covers all 40 plots plus colorblind findings"
  artifacts:
    - path: ".planning/phases/07-plot-audit/AUDIT-TRENDS.md"
      provides: "Per-plot audit results for all 19 trends_plots.py functions"
      contains: "Color decision"
    - path: ".planning/phases/07-plot-audit/AUDIT-REPORT.md"
      provides: "Consolidated audit report covering all 40 plots plus colorblind accessibility (per D-01)"
      contains: "Executive Summary"
  key_links:
    - from: "AUDIT-REPORT.md"
      to: "AUDIT-LATEST.md"
      via: "Consolidates latest plot audit results"
      pattern: "latest_plots"
    - from: "AUDIT-REPORT.md"
      to: "AUDIT-TRENDS.md"
      via: "Consolidates trends plot audit results"
      pattern: "trends_plots"
    - from: "AUDIT-REPORT.md"
      to: "COLORBLIND-FINDINGS.md"
      via: "Incorporates colorblind accessibility section"
      pattern: "Deuteranopia"
---

<objective>
Audit all 19 trend plot functions in trends_plots.py, then assemble the final consolidated AUDIT-REPORT.md combining latest audits, trends audits, and colorblind findings into one document (per D-01).

Purpose: Completes the AUDIT-01/AUDIT-02 deliverable for trends plots and produces the single AUDIT-REPORT.md that Phases 8 and 9 will consume. The consolidated report is the phase's primary output per D-01.

Output: AUDIT-TRENDS.md (intermediate), AUDIT-REPORT.md (final deliverable per D-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-plot-audit/07-CONTEXT.md
@.planning/phases/07-plot-audit/07-RESEARCH.md
@.planning/phases/07-plot-audit/07-01-SUMMARY.md
@.planning/phases/07-plot-audit/07-02-SUMMARY.md
@.planning/phases/07-plot-audit/COLORBLIND-FINDINGS.md
@.planning/phases/07-plot-audit/AUDIT-LATEST.md
@plots/shared.py (lines 187-228: BRAND_COLORS)
@plots/__init__.py
</context>

<interfaces>
<!-- Key contracts -->

19 trends_plots.py functions to audit:
1. plot_main_graph
2. plot_avg_per_aop
3. plot_network_density
4. plot_author_counts
5. plot_ke_components
6. plot_ke_components_percentage
7. plot_unique_ke_components
8. plot_kes_by_kec_count
9. plot_bio_processes
10. plot_bio_objects
11. plot_aop_property_presence
12. plot_ke_property_presence
13. plot_ker_property_presence
14. plot_stressor_property_presence
15. plot_aop_lifetime
16. plot_entity_completeness_trends
17. plot_aop_completeness_boxplot
18. plot_oecd_completeness_trend
19. plot_ontology_term_growth

Return type conventions:
- Most return tuple[str, str] = (absolute_plot_html, delta_or_percentage_plot_html)
- Some return tuple[str, str, pd.DataFrame] = (abs, delta, data)
- BOTH outputs in the tuple must be audited separately
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Audit all 19 trends_plots.py functions</name>
  <files>.planning/phases/07-plot-audit/AUDIT-TRENDS.md</files>
  <read_first>
    - plots/trends_plots.py (full file -- all 3416 lines, the audit target)
    - plots/shared.py (lines 187-228 for BRAND_COLORS, lines 231-250 for template colorway)
    - .planning/phases/07-plot-audit/COLORBLIND-FINDINGS.md (confusable pairs to cross-reference)
    - .planning/phases/07-plot-audit/07-CONTEXT.md (D-03 rubric, D-04 classification, D-05 color decisions)
    - templates/trends_page.html (to check which functions are wired)
  </read_first>
  <action>
Read every `def plot_*` function in plots/trends_plots.py. For each function, evaluate against the same 8-dimension binary rubric used in Plan 02.

**CRITICAL: Trends functions return tuples.** Most return (absolute_plot, delta_plot) or (absolute_plot, percentage_plot) or (absolute_plot, delta_plot, dataframe). BOTH plot outputs must be evaluated. Score each output separately in the rubric notes, but produce ONE classification and ONE color decision per function.

**Rubric dimensions (same as Plan 02, per D-03):**
1. **Color correctness** (HEAVY): Check color mechanism for BOTH outputs.
   - Time series with multiple entity lines (e.g., AOPs, KEs, KERs, Stressors) -> categorical palette is appropriate
   - Single-metric trends (e.g., network density over time) -> single #307BBF
   - Property presence plots using type_colors -> categorical with type_colors mapping
   - OECD-related -> oecd_status mapping
   - Check: Does the function use go.Scatter with explicit line/marker colors, or px.line with color=?

2. **Chart type appropriateness** (HEAVY):
   - Time series -> line chart (correct for trends)
   - Distribution over time -> box plot
   - Composition over time -> stacked area or stacked bar

3-8. Same as Plan 02 (data table, title, axes, legend, layout, tooltips).

**Classification (per D-04):** Same rules: FIX NOW if dim 1 or 2 fails; FIX LATER if only dims 3-8 fail; SKIP if all pass.

**Color decision (per D-05):** Same format: "single #307BBF" or "categorical palette" with rationale.

**Wiring check:** Cross-reference against templates/trends_page.html data-plot-name attributes.

**Colorblind cross-reference:** For categorical plots, note confusable pairs from COLORBLIND-FINDINGS.md.

**Output format for AUDIT-TRENDS.md:**

```markdown
# Plot Audit: trends_plots.py

**Audited:** {date}
**Functions:** 19
**Method:** Code analysis only (per D-06)
**Note:** Trend functions return tuples. Both outputs evaluated; notes distinguish "absolute" vs "delta/percentage" where relevant.

## Summary

| # | Function | Outputs | Classification | Color Decision | Wired |
|---|----------|---------|----------------|----------------|-------|
| 1 | plot_main_graph | abs + delta | ... | ... | yes/no |
...

## Per-Plot Audit

### 1. plot_main_graph

**Returns:** (absolute_html, delta_html, dataframe)

| Dimension | Pass/Fail | Notes |
|-----------|-----------|-------|
| Color correctness | ... | Absolute: {details}. Delta: {details}. |
| Chart type | ... | Absolute: line chart. Delta: bar chart. |
| Data table | ... | {what dataframe is returned/cached} |
| Title quality | ... | Absolute: "{title}". Delta: "{title}". |
| Axis labels | ... | ... |
| Legend clarity | ... | ... |
| Layout & spacing | ... | ... |
| Tooltip quality | ... | ... |

**Classification:** {FIX NOW / FIX LATER / SKIP}
**Color decision:** {single #307BBF / categorical palette}
**Rationale:** {why -- e.g., "4 entity types (AOP, KE, KER, Stressor) require categorical distinction"}
**Wiring:** {wired to trends_page.html / unwired}
**Colorblind note:** {confusable pairs used, or "N/A"}

{Repeat for all 19 functions}
```
  </action>
  <verify>
    <automated>grep -c "^### [0-9]" .planning/phases/07-plot-audit/AUDIT-TRENDS.md</automated>
  </verify>
  <acceptance_criteria>
    - AUDIT-TRENDS.md exists and is non-empty
    - File contains "# Plot Audit: trends_plots.py"
    - File contains exactly 19 per-plot sections (grep -c "^### [0-9]" returns 19)
    - Each section contains "Classification:" with one of: FIX NOW, FIX LATER, SKIP
    - Each section contains "Color decision:"
    - File contains "plot_main_graph" (first function)
    - File contains "plot_ontology_term_growth" (last function)
    - File contains "Returns:" in each per-plot section (documenting tuple outputs)
    - Summary table lists all 19 functions
  </acceptance_criteria>
  <done>All 19 trends_plots.py functions audited with rubric scores, classifications, color decisions, and tuple-output evaluation</done>
</task>

<task type="auto">
  <name>Task 2: Assemble consolidated AUDIT-REPORT.md</name>
  <files>.planning/phases/07-plot-audit/AUDIT-REPORT.md</files>
  <read_first>
    - .planning/phases/07-plot-audit/AUDIT-LATEST.md (latest plot audit results)
    - .planning/phases/07-plot-audit/AUDIT-TRENDS.md (trends plot audit results, just created in Task 1)
    - .planning/phases/07-plot-audit/COLORBLIND-FINDINGS.md (deuteranopia analysis)
    - .planning/phases/07-plot-audit/07-CONTEXT.md (D-01: single markdown file, D-02: markdown only)
  </read_first>
  <action>
Combine AUDIT-LATEST.md, AUDIT-TRENDS.md, and COLORBLIND-FINDINGS.md into a single consolidated AUDIT-REPORT.md (per D-01). This is the primary deliverable of Phase 7.

**Structure (per research recommendation):**

```markdown
# AOP-Wiki RDF Dashboard - Plot Audit Report

**Date:** {date}
**Scope:** All plot functions in plots/latest_plots.py (20) and plots/trends_plots.py (19)
**Method:** Code analysis only (per D-06)
**Rubric:** 8-dimension binary pass/fail (per D-03)

## Executive Summary

- **Total functions audited:** 39
- **Total plot outputs audited:** {N} (trends functions produce 2 outputs each)
- **FIX NOW:** {count} ({list function names})
- **FIX LATER:** {count}
- **SKIP:** {count}
- **Colorblind confusable pairs:** {count} pairs with delta E < 10

## Classification Summary

### FIX NOW
| Function | Module | Failed Dimensions | Color Decision |
|----------|--------|-------------------|----------------|
...

### FIX LATER
| Function | Module | Failed Dimensions | Color Decision |
...

### SKIP
| Function | Module | Color Decision |
...

## All Color Decisions

| # | Function | Module | Color Decision | Rationale |
|---|----------|--------|----------------|-----------|
{All 39 functions listed, sorted by module then name}

## Colorblind Accessibility (Deuteranopia)

{Copy full content from COLORBLIND-FINDINGS.md}

### Affected Plots

{Cross-reference: for each confusable pair, list which specific plot functions use both colors simultaneously. Use color usage data from AUDIT-LATEST.md and AUDIT-TRENDS.md to determine this.}

## Per-Plot Audit: latest_plots.py

{Copy full per-plot sections from AUDIT-LATEST.md}

## Per-Plot Audit: trends_plots.py

{Copy full per-plot sections from AUDIT-TRENDS.md}

## Methodology

- **Rubric:** 8 dimensions per D-03: color correctness, chart type appropriateness, data table usefulness, title quality, axis labels & units, legend clarity, layout & spacing, tooltip quality
- **Classification:** Per D-04: FIX NOW (color or chart type fails), FIX LATER (only light dims fail), SKIP (all pass)
- **Color decisions:** Per D-05: "single #307BBF" or "categorical palette" with rationale
- **Colorblind:** Deuteranopia only per D-09. Vienot 1999 simulation + CIEDE2000 delta E < 10 threshold per D-10. Document-only, no fix recommendations per D-11.
```

**Key assembly tasks:**
1. Compute aggregate counts from AUDIT-LATEST.md and AUDIT-TRENDS.md (count FIX NOW, FIX LATER, SKIP)
2. Build the "All Color Decisions" master table from both audit files
3. Cross-reference confusable colorblind pairs against specific plot functions that use those color combinations
4. Verify all 39 functions are present (20 latest + 19 trends)
  </action>
  <verify>
    <automated>grep -c "^### [0-9]" .planning/phases/07-plot-audit/AUDIT-REPORT.md && grep "Executive Summary" .planning/phases/07-plot-audit/AUDIT-REPORT.md && grep "Colorblind" .planning/phases/07-plot-audit/AUDIT-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - AUDIT-REPORT.md exists in .planning/phases/07-plot-audit/ (per D-01)
    - File contains "# AOP-Wiki RDF Dashboard - Plot Audit Report"
    - File contains "Executive Summary" section
    - File contains "FIX NOW" count and function list
    - File contains "FIX LATER" count
    - File contains "SKIP" count
    - File contains "All Color Decisions" section with a table covering all 39 functions
    - File contains "Colorblind Accessibility" section with deuteranopia findings
    - File contains "Affected Plots" subsection cross-referencing confusable pairs to specific functions
    - File contains per-plot sections totaling 39 (grep -c "^### [0-9]" returns 39)
    - File contains "Methodology" section citing D-03, D-04, D-05, D-06, D-09, D-10, D-11
    - File does NOT contain fix recommendations in the colorblind section (per D-11)
    - File contains both "latest_plots.py" and "trends_plots.py" section headers
  </acceptance_criteria>
  <done>Consolidated AUDIT-REPORT.md exists as a single markdown file containing all 39 plot audits, colorblind findings, executive summary, and master color decision table -- ready for Phase 8 and Phase 9 consumption</done>
</task>

</tasks>

<verification>
- AUDIT-TRENDS.md contains 19 per-plot audit entries
- AUDIT-REPORT.md contains all 39 plots from both modules
- Executive summary has accurate FIX NOW / FIX LATER / SKIP counts
- Every plot has an explicit color decision
- Colorblind findings integrated with per-plot cross-references
- No code changes made to any plot files (documentation-only phase)
</verification>

<success_criteria>
- All 19 trends_plots.py functions evaluated against 8-dimension rubric
- Consolidated AUDIT-REPORT.md is the single deliverable per D-01
- Executive summary provides at-a-glance classification counts
- Master color decision table covers all 39 functions for Phase 8 mechanical execution
- Colorblind affected-plots section maps confusable pairs to specific functions
</success_criteria>

<output>
After completion, create `.planning/phases/07-plot-audit/07-03-SUMMARY.md`
</output>
