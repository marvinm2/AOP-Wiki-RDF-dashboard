---
phase: 10-sparql-transparency
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified: []
autonomous: false
requirements:
  - SPARQL-01
  - SPARQL-02

must_haves:
  truths:
    - "User confirms SPARQL query panel renders correctly on latest page"
    - "User confirms SPARQL query panel renders correctly on trends page"
    - "User confirms Run on Endpoint opens Virtuoso with pre-filled query"
  artifacts: []
  key_links: []
---

<objective>
Verify the SPARQL transparency feature works end-to-end on both latest and trends pages.

Purpose: Human visual confirmation that buttons render correctly, links open Virtuoso with pre-filled queries, and Copy Query works.
Output: User approval of the feature.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sparql-transparency/10-CONTEXT.md
@.planning/phases/10-sparql-transparency/10-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of SPARQL transparency on both pages</name>
  <files>templates/macros/methodology.html, static/css/main.css, app.py, static/data/methodology_notes.json</files>
  <read_first>
    - .planning/phases/10-sparql-transparency/10-01-SUMMARY.md (what was implemented)
  </read_first>
  <action>
Start the Flask app and present the verification checklist to the user. No code changes — this is a visual verification checkpoint.

Run `python app.py` in the background if not already running, then present the how-to-verify steps to the user and wait for approval.
  </action>
  <what-built>
SPARQL transparency feature: every plot with a methodology note now shows "Run on Endpoint" and "Copy Query" buttons inside the SPARQL query accordion. Two previously missing methodology entries (database_summary, ontology_usage) have been added.
  </what-built>
  <how-to-verify>
1. Start the app: `python app.py` (or `flask run`)
2. Open http://localhost:5000 (latest/snapshot page)

**On the Latest page:**
3. Scroll to any plot (e.g., Entity Counts). Click "Methodology" to expand.
4. Click "View SPARQL Query" to expand the nested accordion.
5. Verify: A formatted SPARQL query is visible in a code block.
6. Verify: Below the code block, a blue "Run on Endpoint" button and an outlined "Copy Query" button are visible.
7. Click "Run on Endpoint" — verify a new tab opens with the Virtuoso SPARQL editor and the query is pre-filled in the query box.
8. Click "Copy Query" — verify the button text changes to "Copied!" for 2 seconds, then paste somewhere to confirm the query text was copied.
9. Check the "Core Entity Summary" plot (database_summary) — verify it now has a methodology panel with SPARQL query.
10. Check the "Ontology Usage" plot — verify it now has a methodology panel with SPARQL query.

**On the Trends page:**
11. Navigate to http://localhost:5000/trends
12. Repeat steps 3-8 on any trends plot (e.g., Main Graph Evolution).

**Edge cases:**
13. Verify no broken layouts — buttons should align horizontally, not overflow the panel.
14. Verify the "Run on Endpoint" button hover changes from blue (#307BBF) to dark purple (#29235C).
  </how-to-verify>
  <verify>
    <automated>grep -c "sparql-run-btn" templates/macros/methodology.html && grep -c "sparql_endpoint=Config.SPARQL_ENDPOINT" app.py</automated>
  </verify>
  <resume-signal>Type "approved" if everything works, or describe any issues found.</resume-signal>
  <acceptance_criteria>
    - User confirms "Run on Endpoint" button is visible on at least 3 plots across both pages
    - User confirms clicking "Run on Endpoint" opens Virtuoso in a new tab with query pre-filled
    - User confirms "Copy Query" copies SPARQL text to clipboard
    - User confirms database_summary and ontology_usage plots now have methodology panels
  </acceptance_criteria>
  <done>User has visually confirmed SPARQL transparency works on both latest and trends pages. Run on Endpoint opens Virtuoso correctly. Copy Query works. No layout issues.</done>
</task>

</tasks>

<verification>
User approval of visual verification checkpoint.
</verification>

<success_criteria>
- User types "approved" after verifying SPARQL transparency on both pages
- No issues reported with button rendering, link behavior, or copy functionality
</success_criteria>

<output>
After completion, create `.planning/phases/10-sparql-transparency/10-02-SUMMARY.md`
</output>
