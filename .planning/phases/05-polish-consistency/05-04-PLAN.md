---
phase: 05-polish-consistency
plan: 04
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified:
  - templates/landing.html
  - templates/about.html
  - app.py
  - static/css/main.css
autonomous: false
requirements:
  - Brand alignment
  - UX polish
  - Visual consistency

must_haves:
  truths:
    - "Landing page serves as a navigation hub with icon+description cards for each section"
    - "Landing page shows latest version number and headline entity counts from live data"
    - "Landing page has expandable AOP-Wiki intro collapsed by default"
    - "Landing page has VHP4Safety logo and funding acknowledgment"
    - "About page is accessible at /about with project info, funding, SPARQL endpoint link, and issue-reporting link"
    - "All pages render correctly with consistent visual identity"
  artifacts:
    - path: "templates/landing.html"
      provides: "Navigation hub landing page extending base.html"
      contains: "{% extends 'base.html' %}"
    - path: "templates/about.html"
      provides: "About page with project info, funding, contact"
      contains: "{% extends 'base.html' %}"
    - path: "app.py"
      provides: "/about route and updated landing route with live data context"
      contains: "def about"
  key_links:
    - from: "app.py"
      to: "templates/about.html"
      via: "/about route renders about.html"
      pattern: "render_template.*about.html"
    - from: "app.py"
      to: "templates/landing.html"
      via: "Landing route passes entity counts and version"
      pattern: "render_template.*landing.html"
    - from: "templates/landing.html"
      to: "templates/base.html"
      via: "Jinja2 extends"
      pattern: "extends.*base.html"
---

<objective>
Redesign the landing page as a navigation hub and create a dedicated About page, completing the dashboard's visual identity and user experience.

Purpose: Transform the landing page from its current hero-section design into a clean navigation hub that guides users to the right section, shows light live data (version + entity counts), and provides an expandable AOP-Wiki introduction. Add an About page with project information, funding acknowledgment, and contact links. This completes all 6 Phase 5 success criteria.

Output: Redesigned landing.html, new about.html, /about Flask route, updated landing route with live data, CSS styling for cards and about page.
</objective>

<execution_context>
@/home/marvin/.claude/get-shit-done/workflows/execute-plan.md
@/home/marvin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-polish-consistency/05-RESEARCH.md
@.planning/phases/05-polish-consistency/05-01-SUMMARY.md
@.planning/phases/05-polish-consistency/05-02-SUMMARY.md
@.planning/phases/05-polish-consistency/05-03-SUMMARY.md
@templates/landing.html
@templates/base.html
@app.py
@static/css/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign landing page as navigation hub with live data</name>
  <files>templates/landing.html, app.py, static/css/main.css</files>
  <action>
**1. Update the landing route in `app.py`:**
The existing `/` route currently renders `landing.html`. Update it to pass light live data to the template:
- Call `get_latest_version()` to get the current version string
- Get headline entity counts from `_plot_data_cache.get('latest_entity_counts')` — this is the correct cache key (stored by `plot_latest_entity_counts()` in latest_plots.py). The cached value is a pandas DataFrame with columns like 'Entity' and 'Count'. Extract a simple dict of entity name → count from it (e.g., `entity_counts = dict(zip(df['Entity'], df['Count']))` if the DataFrame exists, else None).
- Pass these as template context: `version=latest_version, entity_counts=entity_counts, active_page='home'`

Keep it lightweight — no additional SPARQL queries. Use cached data that's already computed at startup.

**2. Redesign `templates/landing.html`:**
Replace the current hero-section design. The new landing page extends base.html:

```html
{% extends 'base.html' %}

{% block title %}AOP-Wiki RDF Dashboard{% endblock %}

{% block content %}
{# Navigation Hub Cards #}
<section class="landing-hero">
    <h1 class="landing-title">AOP-Wiki RDF Dashboard</h1>
    <p class="landing-subtitle">
        Explore the Adverse Outcome Pathway knowledge base through interactive
        visualizations, network analysis, and historical trend monitoring.
    </p>
    {% if version %}
    <div class="landing-stats">
        <span class="stat-badge">Latest version: <strong>{{ version }}</strong></span>
        {% if entity_counts %}
        {% for entity, count in entity_counts.items() %}
        <span class="stat-badge">{{ entity }}: <strong>{{ count }}</strong></span>
        {% endfor %}
        {% endif %}
    </div>
    {% endif %}
</section>

<section class="landing-cards">
    <a href="/snapshot" class="landing-card">
        <div class="card-icon"><!-- SVG or unicode icon for snapshot --></div>
        <h2>Database Snapshot</h2>
        <p>Current database metrics, entity distributions, data quality indicators,
           and completeness analysis for the selected AOP-Wiki version.</p>
        <span class="card-action">View Snapshot &rarr;</span>
    </a>
    <a href="/trends" class="landing-card">
        <div class="card-icon"><!-- SVG or unicode icon for trends --></div>
        <h2>Historical Trends</h2>
        <p>Evolution and growth patterns across 30+ quarterly releases since 2018.
           Track entity growth, author contributions, and annotation progress.</p>
        <span class="card-action">View Trends &rarr;</span>
    </a>
    <a href="/network" class="landing-card">
        <div class="card-icon"><!-- SVG or unicode icon for network --></div>
        <h2>Network Analysis</h2>
        <p>Interactive Key Event network with centrality metrics, PageRank scores,
           and community detection. Explore structural relationships.</p>
        <span class="card-action">View Network &rarr;</span>
    </a>
</section>

{# Expandable AOP-Wiki Introduction #}
<section class="landing-info">
    <details class="aopwiki-intro">
        <summary>What is AOP-Wiki?</summary>
        <div class="intro-content">
            <p>The AOP-Wiki is the primary repository for Adverse Outcome Pathways (AOPs),
               which describe mechanistic links between molecular initiating events and
               adverse outcomes relevant to risk assessment. AOPs consist of Key Events (KEs)
               connected by Key Event Relationships (KERs), forming directed networks that
               capture biological knowledge.</p>
            <p>This dashboard monitors the RDF (Resource Description Framework) version of
               AOP-Wiki data, enabling SPARQL-based analytics across quarterly snapshot releases.
               The RDF data captures AOPs, Key Events, Key Event Relationships, Stressors,
               biological processes, cellular components, and ontology annotations.</p>
            <p><a href="https://aopwiki.org" target="_blank" rel="noopener">Visit AOP-Wiki</a> |
               <a href="https://aopwiki.cloud.vhp4safety.nl" target="_blank" rel="noopener">SPARQL Endpoint</a></p>
        </div>
    </details>
</section>
{% endblock %}
```

Design notes:
- Cards are styled as clickable `<a>` elements with hover elevation effect
- Use unicode symbols for card icons (no emoji): a bar chart symbol for snapshot, a line graph for trends, a nodes symbol for network — OR use simple inline SVG icons
- Stats badge shows version number from live data
- AOP-Wiki intro uses native `<details>/<summary>` for zero-JS collapse (same pattern as methodology notes from Phase 02)
- Landing page does NOT have a version selector (that's only on /snapshot)
- The old `data-info-section` and `footer` from the current landing.html are removed — base.html provides the footer

**3. Add landing page CSS to `static/css/main.css`:**
Add rules for `.landing-hero`, `.landing-title`, `.landing-subtitle`, `.landing-stats`, `.stat-badge`, `.landing-cards`, `.landing-card`, `.card-icon`, `.card-action`, `.landing-info`, `.aopwiki-intro`.

Card styles:
- 3-column grid on desktop, single column on tablet/mobile
- White background, subtle shadow, border-radius
- Hover: slight shadow increase + translateY(-2px) for lift effect
- Card text in `var(--color-text)`, card action in `var(--color-cta)` (magenta)
- No text-decoration on card links

Hero styles:
- Centered text, generous top padding
- Title in `var(--color-primary)`, large size
- Subtitle in `var(--color-text-secondary)`
- Stats badge: inline-block with light background border

Info section:
- Same styling as methodology notes `<details>` elements for consistency

Responsive:
- `@media (max-width: 900px)`: cards go to single column, hero padding reduces

**4. Clean up old landing page CSS:**
Remove or update any CSS rules that were specific to the old hero-section landing page design (`.hero-section`, `.hero-content`, `.action-buttons`, `.action-btn`, `.data-info-section`, `.info-card`) — but ONLY if these classes are no longer used in any template. Check with grep first.
  </action>
  <verify>
1. `grep "extends.*base.html" templates/landing.html` confirms template inheritance
2. `grep "landing-card" templates/landing.html` confirms card structure
3. `grep "aopwiki-intro" templates/landing.html` confirms expandable intro
4. `grep "active_page" app.py | grep "home"` confirms landing route passes active_page
5. `grep "entity_counts" templates/landing.html` confirms entity counts are rendered in the template
6. `grep "latest_entity_counts" app.py` confirms correct cache key is used
7. `python -c "from app import app; print('OK')"` succeeds
  </verify>
  <done>Landing page redesigned as navigation hub with 3 section cards (Snapshot, Trends, Network), live version display with headline entity counts from cached data, expandable AOP-Wiki intro, extends base.html with VHP4Safety footer. Landing route passes cached version and entity_counts dict extracted from _plot_data_cache['latest_entity_counts'] DataFrame.</done>
</task>

<task type="auto">
  <name>Task 2: Create About page and finalize CSS polish</name>
  <files>templates/about.html, app.py, static/css/main.css</files>
  <action>
**1. Create `/about` route in `app.py`:**
Add a new route:
```python
@app.route('/about')
def about():
    return render_template('about.html', active_page='about')
```

Place it near the other page routes (after `/network`, before API routes).

**2. Create `templates/about.html`:**
```html
{% extends 'base.html' %}

{% block title %}About - AOP-Wiki RDF Dashboard{% endblock %}

{% block content %}
<section class="about-page">
    <h1>About the AOP-Wiki RDF Dashboard</h1>

    <div class="about-section">
        <h2>Project Overview</h2>
        <p>The AOP-Wiki RDF Dashboard provides interactive visualizations and analytics
           for the Adverse Outcome Pathway (AOP) knowledge base. It monitors quarterly
           RDF snapshot releases, enabling researchers, curators, and regulators to
           explore how the AOP-Wiki database evolves over time.</p>
        <p>The dashboard queries the AOP-Wiki SPARQL endpoint to generate real-time
           visualizations of entity counts, data quality metrics, network topology,
           and historical trends across 30+ database versions since April 2018.</p>
    </div>

    <div class="about-section">
        <h2>Features</h2>
        <ul>
            <li><strong>Database Snapshot:</strong> Current entity counts, distributions,
                completeness metrics, and data quality analysis for any version</li>
            <li><strong>Historical Trends:</strong> Growth patterns, annotation progress,
                and property coverage evolution across all releases</li>
            <li><strong>Network Analysis:</strong> Interactive Key Event network with
                centrality metrics, PageRank, and community detection</li>
            <li><strong>Data Export:</strong> CSV, PNG, and SVG downloads for all
                visualizations with version-tagged filenames</li>
        </ul>
    </div>

    <div class="about-section">
        <h2>Data Source</h2>
        <p>All data is sourced from the AOP-Wiki RDF triplestore via SPARQL queries.</p>
        <p><a href="https://aopwiki.cloud.vhp4safety.nl" target="_blank" rel="noopener"
              class="about-link">AOP-Wiki SPARQL Endpoint</a></p>
        <p><a href="https://aopwiki.org" target="_blank" rel="noopener"
              class="about-link">AOP-Wiki Website</a></p>
    </div>

    <div class="about-section">
        <h2>Funding &amp; Acknowledgment</h2>
        <p>This dashboard is developed within the
           <a href="https://vhp4safety.nl" target="_blank" rel="noopener">VHP4Safety</a>
           project (Virtual Human Platform for Safety Assessment), funded by the
           Dutch Research Council (NWO).</p>
    </div>

    <div class="about-section">
        <h2>Contact &amp; Feedback</h2>
        <p>Found a bug or have a feature request?
           <a href="https://github.com/marvinm2/AOP-Wiki-RDF-dashboard/issues"
              target="_blank" rel="noopener" class="about-link">Open an issue on GitHub</a></p>
        <p>Source code:
           <a href="https://github.com/marvinm2/AOP-Wiki-RDF-dashboard"
              target="_blank" rel="noopener" class="about-link">GitHub Repository</a></p>
    </div>
</section>
{% endblock %}
```

**3. Add About page CSS to `static/css/main.css`:**
Add rules for `.about-page`, `.about-section`, `.about-link`:
- `.about-page`: max-width 800px, centered, padding
- `.about-section`: margin-bottom 2rem, bottom border for separation
- `.about-link`: styled with `var(--color-blue)`, underline on hover
- Headings in `var(--color-primary)`

**4. Final CSS cleanup and responsive polish:**
- Remove old unused CSS classes from the previous landing page design IF they are confirmed unused (grep templates for `.hero-section`, `.action-btn`, `.data-info-section`, `.info-card`, `.page-navigation` usage)
- Ensure all remaining hardcoded color hex values in main.css that correspond to brand colors are replaced with `var()` references (e.g., `#29235C` → `var(--color-primary)`, `#E6007E` → `var(--color-cta)`)
- Verify responsive `@media (max-width: 900px)` rules cover:
  - Navigation wrapping on narrow screens
  - Landing cards going single column
  - About page padding reduction
  - Plot containers allowing full width

**5. Redirect legacy `/old-dashboard` and `/dashboard` routes to `/snapshot`:**
The routes `/old-dashboard` (function `index()`) and `/dashboard` (function `dashboard()`) both render `index.html` with the old tabbed layout. These are legacy routes that will look inconsistent after Phase 5 (old styling while all other routes use the new brand identity). Replace both route handlers with redirects to `/snapshot`:

```python
@app.route("/old-dashboard")
def index():
    """Redirect legacy old-dashboard route to the modern snapshot page."""
    return redirect(url_for('database_snapshot'))

@app.route("/dashboard")
def dashboard():
    """Redirect legacy dashboard route to the modern snapshot page."""
    return redirect(url_for('database_snapshot'))
```

This ensures users with old bookmarks land on the modern Database Snapshot page instead of seeing an unstyled legacy page. The `index.html` template file itself can remain in the repository (no file deletion) — it simply will no longer be served by any route.
  </action>
  <verify>
1. `curl -s http://localhost:5000/about` returns 200 (if server is running) OR `python -c "from app import app; c = app.test_client(); r = c.get('/about'); print(r.status_code)"` returns 200
2. `grep "extends.*base.html" templates/about.html` confirms inheritance
3. `grep "def about" app.py` confirms route exists
4. `grep -c "var(--color" static/css/main.css` shows increased use of CSS variables
5. `grep "active_page.*about" app.py` confirms active_page passed
6. `python -c "from app import app; c = app.test_client(); r = c.get('/old-dashboard'); print(r.status_code, r.headers.get('Location', ''))"` returns 302 with /snapshot redirect
7. `python -c "from app import app; c = app.test_client(); r = c.get('/dashboard'); print(r.status_code, r.headers.get('Location', ''))"` returns 302 with /snapshot redirect
  </verify>
  <done>About page exists at /about with project overview, features, data source, funding, and contact sections. Old landing page CSS cleaned up. Hardcoded brand colors replaced with CSS variables throughout main.css. Basic responsive styles work for tablet screens. Legacy routes /old-dashboard and /dashboard redirect to /snapshot instead of rendering unstyled index.html.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete Phase 5 overhaul</name>
  <action>Human verifies the full visual identity overhaul across all pages.</action>
  <what-built>
Complete visual identity overhaul of the AOP-Wiki RDF Dashboard:
1. Central color system (Python BRAND_COLORS + CSS custom properties)
2. Plotly custom template applied to all ~50 plots (white backgrounds, brand colors, right-side legends, hover toolbars)
3. Redesigned navigation bar with active states and version selector (snapshot page only)
4. Redesigned footer with VHP4Safety branding, funding acknowledgment, and links
5. Landing page as navigation hub with section cards, live version data, and expandable AOP-Wiki intro
6. About page with project info, funding, and contact links
7. All pages use Jinja2 template inheritance from base.html
8. Basic tablet responsive layout
  </what-built>
  <how-to-verify>
Start the server with `python app.py` and verify:

1. **Landing page** (http://localhost:5000/):
   - Shows 3 section cards (Database Snapshot, Historical Trends, Network Analysis)
   - Shows latest version number
   - "What is AOP-Wiki?" section is collapsed by default, expands on click
   - Navigation bar at top with all 4 section links
   - Footer with VHP4Safety branding and funding note

2. **Database Snapshot** (http://localhost:5000/snapshot):
   - Version selector appears in navigation bar (not in page content)
   - Plots load with white backgrounds and brand colors
   - Legends on right side, toolbars hidden until hover
   - All existing functionality works: lazy loading, version switching, CSV download, raw data tables

3. **Historical Trends** (http://localhost:5000/trends):
   - No version selector in nav (correct — it's trends)
   - All trend plots have consistent styling
   - Legends on right side, white backgrounds

4. **Network Analysis** (http://localhost:5000/network):
   - Page renders correctly with navigation
   - Network graph and metrics tabs work

5. **About page** (http://localhost:5000/about):
   - Project info, features, data source, funding, contact links all present
   - Navigation "About" link is highlighted as active

6. **Cross-cutting:**
   - Navigation active state highlights correctly on each page
   - Footer is consistent across all pages
   - On a tablet-width browser (900px or less), content is readable and navigation wraps appropriately
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks good, or describe any visual issues to fix.</resume-signal>
</task>

</tasks>

<verification>
1. All 6 Phase 5 success criteria verified:
   - SC1: All plots use brand colors from central system (grep for template="plotly_white" returns 0)
   - SC2: Landing page has cards, live data, expandable intro, funding ack
   - SC3: Plots have white backgrounds, right legends, hover toolbars, consistent typography
   - SC4: Header/footer redesigned; version selector in nav bar
   - SC5: About page accessible at /about with nav link
   - SC6: Basic tablet responsive (media queries present)
2. `python -c "from app import app; print('OK')"` succeeds
3. All templates extend base.html (except trends.html partial and index.html legacy)
4. Zero `template="plotly_white"` in plot files
5. CSS custom properties defined and used throughout
</verification>

<success_criteria>
- Landing page is a navigation hub, not a data dashboard
- About page exists at /about with project info and contact links
- All pages share navigation and footer via template inheritance
- Human verification confirms visual consistency and functionality
- No regressions in lazy loading, version switching, CSV export, network graph
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-consistency/05-04-SUMMARY.md`
</output>
